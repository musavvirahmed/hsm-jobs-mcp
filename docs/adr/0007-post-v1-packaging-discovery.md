# Post-v1 README and MCP discovery site

After MCP v1 is usable, **hsm-jobs-mcp** ships product packaging as a kennismigrant-first README plus a short HTML **discovery page** at `GET /` on the same origin as Streamable HTTP `/mcp` — connect instructions (first-try **`hsm-jobs` only**; Claude Code, then GitHub Copilot CLI, then any-client JSON; muted footnote pointing register-only asks to sibling **hsm-mcp**), v1-only job-shaped example asks, freshness via `/health` and **`get_index_status`**. Not a jobs portal. Gates: **A0** local product README, **A1** real public origin + `GET /`, **B** public GitHub source link when the repo opens (does not block A0/A1). Architecture mermaid/path table must match the deployed system. Honesty / reading-the-answers detail lives on the README, not on `/`. Tests assert HTTP discovery behaviour and README sections — not CSS.

**Amended (Simplify Connect copy):** dropped required dual-attach of `ind-sponsors` from Connect hero snippets; clients *may* still attach hsm-mcp for register-only questions. Dropped the reading-the-answers box from `/`.

**Considered options:** browser Opening search / sponsor directory on `/`; JSON-only root with no human HTML; coupling first public `/mcp` to open-sourcing; keeping “plan, don’t do” README after v1 exists; inventing hsm-mcp’s `30/min` rate limit; example asks for “last 24h” or title totals; `get_job` URL prompts on the discovery page; rewriting `AGENTS.md` in this ticket; Rentman golden-test myth on `/` or README; keeping required dual-attach and the `/` reading-gist for first-try onboarding — rejected so packaging stays MCP-first, honest to the v1 contract, and shippable while the repo stays private.
